//create a function that reaches out to the sql data base and return records in descending order

//function to find all records for car
db.Automate.findAll().then(services => {
    // projects will be an array of all instances
    return serviceItems
  })
//list record and description
 
//service

//gas

db.Automate.findAll({ where: { name: 'fuel' } }).then(fuels => {
    // projects will be an array of Project instances with the specified name
    return fuelItems

  })



